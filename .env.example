# Copy to .env.local for CLI deploys if needed — never commit real values.
# GitHub Actions uses repository secrets with the same names.

# Create a Vercel token: https://vercel.com/account/tokens
VERCEL_TOKEN=

# Project settings → General → Project ID / Team ID (org)
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=
