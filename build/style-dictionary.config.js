/**
 * Style Dictionary entry point (spec §6).
 * Task 1 scaffold only — the real token pipeline is Task 2.
 * Exits successfully so `pnpm tokens:build` / script hooks do not fail yet.
 */
console.log(
  "[tokens:build] Placeholder — token JSON and Style Dictionary pipeline land in Task 2.",
);
